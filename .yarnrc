# Yarn cache lives on the same volume as node_modules (Desktop/iCloud), so
# hardlinks work within one filesystem. Without this, yarn caches to
# ~/Library/Caches which is a different APFS volume from ~/Desktop, causing
# silent hardlink failures that leave packages with missing files.
cache-folder ".yarn-cache"
