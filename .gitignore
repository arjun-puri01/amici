node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env
.env.local

# Native build output (generated by expo prebuild / expo run:ios)
ios/
android/

# npm — not used, prefer yarn
package-lock.json
.npmrc

# Yarn local cache (kept on same volume as node_modules to avoid hardlink failures)
.yarn-cache/
